argoApps:
  applications:
    test-kunde-applications:
      path: apps
      targetRevision: test
      valueFiles:
        - values.yaml
        - values-test.yaml
      sync: 
        auto: true

    test-website:
      path: test-website
      targetRevision: test
      valueFiles:
        - values.yaml
        - values-test.yaml
      sync: 
        auto: true
