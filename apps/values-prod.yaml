argoApps:
  applications:
    test-kunde-applications:
      path: apps
      targetRevision: prod
      valueFiles:
        - values.yaml
        - values-prod.yaml

    test-website:
      path: test-website
      targetRevision: prod
      valueFiles:
        - values.yaml
        - values-prod.yaml
