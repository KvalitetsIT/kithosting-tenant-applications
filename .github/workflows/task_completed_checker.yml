name: 'PR Checklist Completed Check'
on:
  pull_request:
    types: [opened, edited]

jobs:
  checklist-check:
    runs-on: ubuntu-latest
    steps:
      - uses: kentaro-m/task-completed-checker-action@v0.1.2
        with:
          repo-token: "${{ secrets.GITHUB_TOKEN }}"